# discbot-structure (OUTDATED)
This is the basic code to get your bot started!

## Use this instead
use 
`python -m discord newbot [--prefix <prefix>] [--sharded] [--no-git] name [directory]`

for cogs
`python -m discord newcog [--full] name [directory]`
